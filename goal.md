```text
# Goal：逆向修改 Codex Desktop，在顶部标题栏加入额度进度组件

你需要对我本机安装的 Codex Desktop 应用进行分析、逆向和修改，在 Codex 主窗口顶部标题栏的中央空白区域加入一个原生风格的额度进度组件，用于显示当前 Codex 使用额度、剩余比例、额度重置时间和预计耗尽时间。

本任务不是开发官方 Codex Plugin，也不是开发独立悬浮窗口。必须直接修改或注入 Codex Desktop 客户端，使组件成为 Codex 窗口内部 UI 的一部分。

---

## 一、工作原则

1. 开始工作前先读取当前工作目录及上级目录中适用的 `AGENTS.md`，并严格遵守其中要求。
2. 先分析本机 Codex Desktop 的实际版本、安装方式、技术栈、资源结构和启动流程，再决定修改方案。
3. 不允许根据历史版本、网络文章或他人项目直接假设当前客户端结构。
4. 所有结论必须基于本机实际文件、运行行为、窗口结构、网络请求、IPC 和界面状态进行验证。
5. 优先选择可维护、可回滚、低侵入、更新后可重新应用的方案。
6. 不得上传、泄露或长期保存用户的登录凭据、Cookie、访问令牌、会话令牌或其他敏感认证信息。
7. 不得将 OpenAI 原始应用文件、完整解包内容、专有代码或二进制资源提交到公开仓库。
8. 修改失败时必须 fail-fast，不允许静默忽略关键错误，也不允许显示伪造数据。
9. 不允许使用 Computer Use。使用命令行、脚本、文件分析、调试接口和必要的本地工具完成。
10. 不要只输出分析报告，必须完成可运行的实际修改、安装、卸载和恢复流程。
11. 在动手修改前先建立原始文件备份和可验证的回滚路径。
12. 不得为了完成该功能而破坏 Codex Desktop 原有的签名、安全边界、自动更新或主业务功能，除非确有必要且已明确说明影响。
13. 所有无法确认的字段、接口和行为必须通过本机实际运行验证，不能自行补全或猜测。

---

## 二、核心目标

在 Codex Desktop 主窗口顶部标题栏的中央空白区域增加一个紧凑额度组件。

目标位置要求：

- 位于顶部菜单栏右侧
- 位于 Windows 窗口控制按钮左侧
- 位于标题栏内部
- 位于当前没有其他控件占用的中央横向空白区域
- 不得放置在聊天页面底部
- 不得放置在输入框附近
- 不得放置在第二行工具栏
- 不得使用独立悬浮窗口
- 不得通过覆盖层伪装成标题栏组件
- 不得增加第二层标题栏

标题栏组件至少显示：

- 当前主要额度窗口名称
- 剩余额度百分比
- 横向进度条
- 预计耗尽时间
- 数据状态

空间足够时额外显示：

- 额度重置时间
- 周额度
- Credits
- 最后更新时间

示例效果：

`5h 68%  ███████████░░░  预计 3h12m`

或：

`5h 68% · 预计 3h12m · 06:00 重置`

如果存在多个额度窗口，应支持同时识别并在详情面板中展示，例如：

- 5 小时额度
- 周额度
- Credits
- 模型特定额度
- 云任务额度
- 客户端实际存在的其他额度类型

默认突出显示最可能先耗尽或最先触发限制的额度窗口。

---

## 三、标题栏位置与布局要求

额度组件必须位于 Codex Desktop 顶部标题栏中间的可用空白区域。

具体要求：

- 位于“文件、编辑、视图、帮助”等菜单右侧
- 位于最小化、最大化、关闭按钮左侧
- 不得覆盖任何现有菜单
- 不得覆盖窗口控制按钮
- 不得挤压系统窗口按钮
- 不得改变现有菜单的位置
- 不得改变窗口整体高度
- 不得增加标题栏高度
- 不得导致标题栏上下错位
- 不得破坏原有窗口边框
- 不得影响标题栏双击最大化或还原
- 不得影响窗口贴靠
- 不得影响窗口拖动
- 不得影响右键标题栏系统菜单

组件应尽可能位于可用区域的视觉中心，但必须基于左右控件的实际宽度动态计算安全区域。

不得简单使用固定像素位置，因为以下变量可能变化：

- Windows 显示缩放比例
- 窗口宽度
- 菜单文字宽度
- Codex 语言
- Codex 版本
- 主题
- 系统字体
- 多显示器 DPI
- 窗口最大化状态
- Codex 标题栏结构

当标题栏空间不足时，组件需要自动收缩，不得覆盖其他控件。

收缩优先级：

1. 保留剩余百分比
2. 保留主额度名称
3. 保留进度条
4. 保留预计耗尽时间
5. 隐藏重置时间
6. 隐藏次要额度
7. 切换为极简模式

极简模式可以只显示：

`5h 68%`

或：

`68%`

当安全空间不足时，允许临时隐藏整个组件，但不得与其他标题栏控件重叠。

---

## 四、标题栏拖动与系统行为

标题栏通常包含系统拖动区域，因此必须正确区分可交互区域和拖动区域。

要求：

- 额度组件文字和进度条区域可以点击
- 额度组件本身必须是非拖动区域
- 组件周围未占用区域仍然可以拖动窗口
- 不得将整个标题栏设置为非拖动区域
- 不得导致窗口无法移动
- 不得破坏双击标题栏最大化和还原
- 不得破坏 Windows Snap Layout
- 不得破坏右键标题栏系统菜单
- 不得破坏最小化、最大化和关闭按钮
- 不得影响窗口边缘缩放
- 不得影响多显示器拖动
- 不得影响 Windows 键盘窗口操作
- 不得影响触控板拖动行为

需要先确认 Codex Desktop 实际使用的是：

- Electron 自定义标题栏
- 原生 Windows 标题栏
- WebView 自定义标题栏
- 自定义窗口框架
- 其他机制

不能假设一定使用 CSS `app-region`。必须基于当前版本实际实现进行适配。

---

## 五、标题栏组件内容

默认标题栏组件采用紧凑单行布局。

至少显示：

- 主额度窗口名称
- 剩余比例
- 横向进度条
- 预计耗尽时间

可选显示：

- 重置时间
- 周额度简写
- Credits
- 数据新鲜度
- 当前刷新状态

显示状态示例：

- `5h 68% · 预计 3h12m`
- `5h 68% · 本周期安全`
- `5h 68% · 数据不足`
- `5h 68% · 无明显消耗`
- `5h 68% · 正在刷新`
- `5h 68% · 数据过期`
- `自动刷新已关闭`
- `离线`
- `认证失效`
- `接口不兼容`

不得显示未经确认的字段或推测性名称。

如果客户端实际额度字段名称和预期不同，应以实际含义为准。

---

## 六、详情面板

点击标题栏额度组件时，打开一个轻量详情浮层。

详情浮层必须：

- 属于 Codex 窗口内部
- 不创建独立系统窗口
- 不抢占正常编辑焦点
- 不阻塞主界面
- 不遮挡必要窗口控制按钮
- 支持点击外部关闭
- 支持 Escape 关闭
- 窗口失去焦点时可自动关闭
- 窗口缩小时自动重新定位
- 不超出窗口可视范围

详情面板至少显示：

- 所有识别到的额度窗口
- 每个额度窗口的名称
- 当前剩余比例
- 当前已使用比例
- 进度条
- 当前周期长度
- 重置时间
- 最近更新时间
- 数据新鲜度
- 近期消耗速度
- 周期平均消耗速度
- 预计耗尽时间
- ETA 估算可信度
- 历史样本数量
- 当前数据来源
- 当前轮询频率
- 当前实际轮询频率
- 上次刷新时间
- 上次成功刷新时间
- 下次计划刷新时间
- 请求状态
- 失败次数
- 是否处于退避
- Codex Desktop 版本
- 补丁版本
- 当前兼容性状态
- 最近一次错误摘要

不得在详情面板中显示：

- Cookie
- Access Token
- Authorization Header
- 用户聊天内容
- 完整网络响应
- 账号敏感标识
- 内部认证材料

---

## 七、视觉要求

组件必须与 Codex Desktop 当前标题栏风格一致。

要求：

- 支持亮色主题
- 支持暗色主题
- 跟随 Codex 标题栏背景
- 跟随 Codex 字体
- 跟随 Codex 字号
- 跟随 Codex 边框和间距风格
- 不使用突兀的高饱和颜色
- 默认视觉权重较低
- 鼠标悬停时允许轻微提高对比度
- 进度条高度适合标题栏
- 不使用过厚进度条
- 不显示大型图标
- 不引入明显动画干扰
- 不改变标题栏高度
- 不改变整个窗口尺寸
- 不造成菜单和系统按钮上下偏移
- 不产生额外边框
- 不产生第二层工具栏
- 不显示调试文字
- 不显示伪造占位数据

进度条的颜色应优先复用 Codex 当前主题 token 或现有语义色。

如果没有稳定主题 token，应使用低侵入、低对比、可读性足够的中性色，并同时兼容亮暗主题。

---

## 八、响应式与显示适配

必须适配：

- 窗口最大化
- 窗口还原
- 窄窗口
- 宽窗口
- 高 DPI
- Windows 100%、125%、150%、175%、200% 缩放
- 多显示器
- 不同显示器 DPI
- Codex 亮色主题
- Codex 暗色主题
- Windows 亮色主题
- Windows 暗色主题
- Codex 不同语言
- 菜单文字长度变化
- 系统字体变化
- 标题栏按钮尺寸变化
- 窗口从一个显示器拖动到另一个显示器
- 窗口快速缩放
- 窗口最小化和恢复

不得依赖固定分辨率或固定菜单宽度。

布局计算需要防抖或节流，不能在窗口缩放时产生明显卡顿。

---

## 九、客户端逆向分析要求

必须分析本机 Codex Desktop 的实际结构，包括但不限于：

- 当前 Codex Desktop 版本
- 安装来源
- 安装目录
- 安装包类型
- 应用技术栈
- Electron、WebView、原生框架或其他实现
- 窗口创建流程
- 主进程入口
- 预加载脚本
- 渲染进程
- 前端 bundle
- 动态 chunk
- ASAR 或其他资源封装
- 资源完整性机制
- 内容安全策略
- 自动更新机制
- 标题栏实现方式
- 菜单栏实现方式
- 窗口控制按钮实现方式
- 拖动区域实现方式
- 标题栏组件树
- 稳定 DOM 锚点
- 内部 React、Vue、Svelte 或其他框架结构
- 内部状态管理
- Query Cache
- Redux、Zustand、TanStack Query 或实际使用的其他方案
- IPC 通道
- 主进程服务
- Usage 页面
- 额度请求
- 额度缓存
- 额度字段
- 重置时间字段
- 客户端已有刷新机制

不得仅凭字符串搜索认定某字段含义。

每个关键字段至少需要通过以下一种方式验证：

- 实际界面变化
- 实际额度下降
- 实际重置行为
- 网络响应与 UI 对照
- 内部状态与 UI 对照
- 多次采样
- 客户端设置页结果

---

## 十、额度数据来源

优先复用 Codex Desktop 自身已登录会话和内部额度数据。

数据来源优先级：

1. 客户端内部状态管理或查询缓存
2. Codex Desktop 自身已有的额度刷新结果
3. Codex 自身额度接口响应
4. 现有 IPC 或主进程服务
5. 其他经过实际验证的本地数据来源

优先订阅客户端已经获取的数据，避免额外重复请求。

只有在确认 Codex Desktop 不会主动更新额度，或已有数据无法满足刷新要求时，才允许增加独立轮询。

不得：

- 要求用户手动复制 Token
- 要求用户手动复制 Cookie
- 将认证信息硬编码
- 将认证信息写入配置
- 将认证信息写入日志
- 将认证信息发送给第三方
- 绕过账户权限
- 绕过服务端额度限制
- 把 OpenAI API Platform 用量当成 ChatGPT/Codex 订阅额度
- 使用固定伪造数据
- 在无数据时默认显示 100%
- 无限期显示过期数据
- 在字段含义不确定时自行命名

需要识别和标准化客户端实际存在的字段，包括：

- 已使用比例
- 剩余比例
- 额度窗口类型
- 窗口时长
- 重置时间
- Credits 剩余
- 限制范围
- 模型范围
- 功能范围
- 数据更新时间
- 服务端返回的重试时间
- 数据来源标识

如果字段含义不能可靠确认，应标记为未知，并停止相关计算。

---

## 十一、额度窗口处理

如果存在多个额度窗口，需要分别管理。

至少支持：

- 5 小时额度
- 周额度
- Credits
- 模型特定额度
- 云任务额度
- 客户端实际发现的其他额度

每个额度窗口需要独立保存：

- 唯一标识
- 显示名称
- 剩余比例
- 已使用比例
- 周期长度
- 重置时间
- 最后更新时间
- 历史采样
- 消耗速度
- ETA
- 数据新鲜度
- 错误状态

标题栏默认显示的主额度选择逻辑：

1. 已经耗尽或接近耗尽的额度
2. 预计最先耗尽的额度
3. 最先触发限制的额度
4. 用户手动指定的额度
5. 默认主要额度

用户应可以关闭自动选择，并固定显示某个额度窗口。

---

## 十二、预计耗尽时间

预计耗尽时间必须根据真实历史采样计算。

不能只使用单次额度变化直接预测。

需要实现：

- 本地记录额度采样
- 记录真实采样时间
- 记录每个额度窗口的剩余比例
- 计算近期实际消耗速度
- 计算当前周期平均消耗速度
- 对消耗速度进行平滑
- 排除额度重置
- 排除异常跳变
- 排除重复数据
- 排除失败响应
- 排除过期数据
- 区分短期活跃速度和长期周期速度
- 样本不足时不输出具体 ETA
- 长时间无消耗时标记为无明显消耗
- 数据过期时暂停更新 ETA
- 额度重置后重新建立当前周期基线

ETA 结果至少区分：

- 预计具体耗尽时间
- 预计剩余时长
- 本周期预计不会耗尽
- 样本不足
- 无明显消耗
- 数据过期
- 无法估算

当预计耗尽时间晚于额度重置时间时，应显示：

`本周期预计不会耗尽`

不得继续显示一个晚于重置时间的无意义 ETA。

额度扣除可能不是严格线性的，因此必须把结果表述为统计估算，不能冒充官方预测。

---

## 十三、历史数据

历史数据只保存在本地。

至少记录：

- 采样时间
- 额度窗口标识
- 剩余比例
- 已使用比例
- 重置时间
- 数据来源
- 数据有效性
- 请求状态

不得记录：

- 聊天内容
- Prompt
- Codex 回复
- 用户代码
- 完整网络响应
- Cookie
- Token
- Authorization
- 账号敏感信息

需要支持：

- 自动清理过期历史
- 用户设置历史保留周期
- 手动清空历史
- 数据库损坏检测
- 损坏数据隔离
- 自动重建
- 版本迁移
- 原子写入
- 应用异常退出后的数据一致性

---

## 十四、轮询与刷新策略

额度数据需要支持可配置的自动轮询。

### 14.1 用户设置轮询频率

允许用户设置额度数据轮询频率。

至少提供以下预设：

- 关闭自动轮询
- 30 秒
- 1 分钟
- 2 分钟
- 5 分钟
- 10 分钟
- 15 分钟
- 30 分钟
- 1 小时

同时允许用户输入自定义轮询周期。

边界要求：

- 默认最短轮询周期为 30 秒
- 最长轮询周期为 24 小时
- 默认轮询频率为 5 分钟
- 非法值必须被拒绝
- 空值必须被拒绝
- 负数必须被拒绝
- 超出边界必须被拒绝
- 配置界面必须明确显示单位
- 修改后立即生效
- 不要求重启 Codex Desktop

轮询频率代表“最多按该频率尝试刷新”，不能覆盖：

- 服务端限流
- 请求退避
- 并发控制
- 网络状态
- 窗口状态
- 认证状态
- 兼容性错误

### 14.2 手动刷新

详情面板中需要提供手动刷新按钮。

要求：

- 可以立即请求最新额度
- 显示刷新中状态
- 同一时间只允许一个刷新请求
- 防止快速连续点击
- 定时刷新和手动刷新不能并发
- 手动刷新失败时显示错误
- 手动刷新失败时保留最后一次有效数据
- 显示上次刷新时间
- 显示上次成功刷新时间
- 显示下次计划刷新时间
- 手动刷新不得创建额外永久定时器
- 手动刷新不得绕过认证和服务端限流

### 14.3 动态轮询

轮询应根据 Codex Desktop 实际状态动态调整。

要求：

- Codex 窗口前台活跃时使用用户配置频率
- Codex 窗口处于后台时可以降低频率
- Codex 窗口最小化时可以暂停或显著降低频率
- 系统睡眠时停止轮询
- 系统恢复后重新计算下一次轮询
- 网络离线时暂停正常轮询
- 网络恢复后可触发一次刷新
- Codex 退出时停止所有轮询任务
- 不得因窗口状态切换创建重复定时器

用户可以配置：

- 是否启用后台降频
- 后台降频倍率
- 后台固定轮询周期
- 最小化时是否暂停
- 网络恢复后是否立即刷新
- Codex 启动后是否立即刷新

如果关闭后台降频，则始终按用户设置频率执行，但仍需遵守服务端限流和退避。

### 14.4 请求去重和并发控制

必须保证：

- 同一时间最多一个额度刷新请求
- 定时轮询和手动刷新共享同一请求锁
- 前一个请求未结束时不得启动下一个
- 可以合并短时间内重复刷新请求
- 请求成功后正确释放状态
- 请求失败后正确释放状态
- 请求超时后正确释放状态
- 不允许递归刷新
- 不允许无限请求循环
- 不允许组件重新渲染后创建重复定时器
- 不允许打开多个详情面板后创建重复轮询
- 不允许打开多个 Codex 窗口后无控制地倍增请求

如果多个 Codex 窗口共享同一账户额度，应优先使用单一共享轮询源，并将结果分发给各窗口。

### 14.5 超时和失败退避

额度请求需要明确超时和失败退避机制。

要求：

- 单次请求必须有超时
- 超时后释放刷新锁
- 连续失败时采用受控退避
- 退避时间不得无限增长
- 成功后恢复正常轮询周期
- HTTP 限流时遵守服务端返回的重试时间
- 认证失效时停止高频重试
- 接口字段不兼容时停止自动解析
- 用户手动刷新可绕过当前自动等待时间，但不能绕过并发锁和限流
- 连续失败期间不得高频重复请求

建议支持：

- 初始退避
- 最大退避时间
- 随机抖动
- 成功后重置失败计数
- 用户手动重试

### 14.6 数据新鲜度

状态栏和详情面板需要根据最后一次有效数据时间显示新鲜度。

至少区分：

- 最新
- 正在刷新
- 数据较旧
- 数据已过期
- 离线
- 请求失败
- 接口限流
- 认证失效
- 接口不兼容
- 自动刷新已关闭

数据新鲜度阈值应结合用户设置的轮询周期动态计算。

建议：

- 超过约 2 个轮询周期未更新：数据较旧
- 超过约 5 个轮询周期未更新：数据已过期

数据过期后：

- 可以保留最后一次有效数值
- 必须明确标记为过期
- 不得显示为实时数据
- 不得继续更新 ETA
- 不得基于过期数据生成新的消耗预测

### 14.7 ETA 与轮询关系

ETA 计算必须使用真实采样时间，不得假设固定采样间隔。

要求：

- 修改轮询频率后不清空历史
- 使用真实时间差计算消耗速度
- 不把高频重复无变化采样错误解释为消费速度下降
- 不把长时间离线样本纳入正常速度估算
- 数据过期后暂停更新 ETA
- 自动轮询关闭时，只在手动刷新后更新 ETA
- 额度重置后重新建立周期基线
- 不同轮询频率下计算结果应保持一致性

### 14.8 优先复用 Codex 原有刷新

如果 Codex Desktop 本身已有额度刷新机制，应优先：

- 订阅其结果
- 复用其状态
- 监听其缓存
- 读取其 Query Store
- 读取其 IPC 结果

不得在客户端已经请求额度时再重复发起完全相同的请求。

独立轮询只应作为以下情况的补充：

- 原客户端不主动刷新
- 原客户端仅在 Usage 页面打开时刷新
- 原客户端刷新频率无法满足用户配置
- 原客户端不暴露足够新鲜的数据

即使使用独立轮询，也不能影响 Codex 原有请求。

---

## 十五、配置功能

需要提供配置入口。

优先集成到 Codex 设置页面；如果设置页注入不稳定，可以在详情面板中提供配置界面。

至少支持：

- 启用或禁用标题栏额度组件
- 选择显示哪些额度窗口
- 选择默认主要额度
- 自动选择最先耗尽额度
- 紧凑模式
- 标准模式
- 是否显示进度条
- 是否显示预计耗尽时间
- 是否显示重置时间
- 是否显示数据状态
- 相对时间或绝对时间
- 设置自动轮询频率
- 关闭自动轮询
- 设置自定义轮询频率
- 是否启用后台降频
- 设置后台降频倍率
- 设置后台固定轮询周期
- 最小化时是否暂停轮询
- 网络恢复后是否立即刷新
- 启动后是否立即刷新
- 设置请求超时时间
- 设置历史保留周期
- 清空历史
- 启用或关闭 ETA
- 启用或关闭调试日志
- 查看兼容性诊断
- 查看当前数据源
- 查看当前实际轮询频率
- 一键恢复默认配置

配置修改应立即生效，不要求重启。

配置必须持久化在用户目录，不得污染 Codex 安装目录中的业务数据。

无效配置应回退到安全默认值，不能导致 Codex 无法启动。

---

## 十六、更新兼容和补丁管理

Codex Desktop 更新后，补丁可能失效，因此必须实现完整兼容性管理。

至少包括：

- 记录当前 Codex 版本
- 记录安装来源
- 记录原始应用资源哈希
- 记录补丁后资源哈希
- 记录补丁版本
- 检测 Codex 是否更新
- 检测补丁是否仍存在
- 检测标题栏锚点是否有效
- 检测组件是否挂载成功
- 检测额度 Hook 是否有效
- 检测配置读取是否正常
- 检测历史存储是否正常
- 检测窗口拖动是否正常
- 检测窗口控制按钮是否正常
- 检测轮询是否重复运行
- 检测接口字段是否变化
- 不兼容时停止注入
- 支持重新应用补丁
- 支持恢复原始文件
- 支持完整卸载
- 修改前自动备份
- 重复安装保持幂等
- 不允许重复插入相同补丁

不得禁用 Codex 自动更新。

如果 Microsoft Store、AppX、签名或 ACL 机制导致无法直接修改原安装目录，可以创建本地可写的补丁副本，但最终运行的仍必须是修改后的 Codex Desktop 应用本体，不能改成外部悬浮窗口。

---

## 十七、回滚和故障恢复

必须提供可靠回滚能力。

要求：

- 修改前备份原始文件
- 备份文件带版本和哈希
- 安装失败自动恢复
- 启动失败能够恢复
- 资源哈希不匹配时停止覆盖
- 卸载时恢复原始文件
- 重复卸载不会破坏应用
- 回滚后 Codex 能正常启动
- 不删除用户聊天、配置或工作区数据
- 不删除 Codex 原始账户数据
- 不修改无关系统设置

所有关键文件替换尽量使用原子操作。

---

## 十八、安全和隐私

必须遵守：

- 所有额度分析在本地完成
- 不接入第三方统计服务
- 不上传额度历史
- 不上传聊天内容
- 不上传 Prompt
- 不上传 Codex 回复
- 不上传用户代码
- 不上传完整网络请求
- 不上传完整网络响应
- 不记录 Cookie
- 不记录 Token
- 不记录 Authorization
- 不记录账户敏感标识
- 默认关闭详细调试日志
- 日志必须自动脱敏
- 历史数据仅保存额度数值和时间
- 不开启不必要的远程调试端口
- 不允许其他本地用户读取敏感数据
- 不绕过服务端权限
- 不绕过服务端限流
- 不修改账户额度
- 不伪造额度返回值

---

## 十九、稳定性要求

任何额度功能异常都不得影响 Codex Desktop 主功能。

必须保证：

- 数据获取失败时 Codex 仍能使用
- 组件渲染失败时主界面仍能加载
- 标题栏注入失败时窗口仍能操作
- 历史数据库损坏时自动隔离或重建
- 无网络时显示离线
- 字段变化时停止解析并报告不兼容
- 不产生无限请求循环
- 不产生重复定时器
- 不产生重复事件监听器
- 不产生明显内存泄漏
- 不产生持续高 CPU
- 不产生持续磁盘写入
- 不阻塞主线程
- 不频繁触发 React 重绘
- 不明显增加启动时间
- 应用关闭时正确释放资源
- 补丁失败时自动回滚
- 标题栏组件失效时主业务仍正常
- 手动刷新和定时刷新不会并发
- 多窗口场景不会失控倍增请求

---

## 二十、诊断能力

需要提供清晰诊断输出。

至少包括：

- Codex Desktop 版本
- 安装来源
- 安装目录
- 应用技术栈
- 补丁版本
- 补丁是否安装
- 原始资源哈希
- 当前资源哈希
- 标题栏注入是否成功
- 标题栏安全区域是否有效
- 窗口拖动是否正常
- 窗口控制按钮是否正常
- 额度数据 Hook 是否成功
- 当前数据来源类型
- 最近有效数据时间
- 当前轮询配置
- 当前实际轮询频率
- 自动轮询是否启用
- 是否后台降频
- 当前请求状态
- 当前失败次数
- 当前退避时间
- 下次计划刷新时间
- 历史存储是否正常
- ETA 样本是否足够
- 是否检测到 Codex 更新
- 当前兼容性状态
- 最近一次错误摘要

诊断信息不得包含敏感认证数据或完整接口响应。

---

## 二十一、交付内容

最终必须交付：

1. 可重复执行的安装方式
2. 可重复执行的卸载方式
3. 原始文件恢复方式
4. Codex 更新后的重新补丁方式
5. 修改后的 Codex 启动方式
6. 完整源代码
7. 架构说明
8. 逆向分析结果
9. 标题栏结构分析
10. 已确认的额度数据结构
11. 额度字段含义验证结果
12. 轮询机制说明
13. ETA 估算逻辑说明
14. 配置说明
15. 安全和隐私说明
16. 更新兼容说明
17. 已知限制
18. 兼容版本范围
19. 故障排查说明
20. 自动化测试
21. 手工验收记录
22. 回滚验证记录

不得只交付一次性修改后的二进制文件。

不得提交或分发 OpenAI 原始专有资源。

---

## 二十二、测试要求

至少测试以下场景：

### 标题栏

- 标题栏组件正确显示
- 组件位于顶部中央空白区域
- 不与菜单重叠
- 不与窗口控制按钮重叠
- 不增加标题栏高度
- 窗口仍可拖动
- 双击标题栏仍可最大化和还原
- 最小化正常
- 最大化正常
- 关闭正常
- Windows Snap Layout 正常
- 右键标题栏系统菜单正常
- 窄窗口自动收缩
- 极窄窗口自动隐藏或极简显示
- 宽窗口显示完整信息
- 高 DPI 正常
- 多显示器正常
- 显示器之间拖动正常
- 亮色主题正常
- 暗色主题正常
- 不同语言菜单正常

### 详情面板

- 点击标题栏组件可以打开
- 点击外部关闭
- Escape 关闭
- 窗口缩放时正确定位
- 不抢占输入焦点
- 不超出窗口范围

### 数据

- Usage 页面从未打开
- Usage 页面打开后获取数据
- 客户端已有缓存
- 客户端无缓存
- 网络断开
- 网络恢复
- 接口返回错误
- 接口限流
- 认证失效
- 字段变化
- 多额度窗口
- 额度正常下降
- 额度重置
- 数据长时间不变化
- 数据过期

### ETA

- 样本不足
- 正常消耗
- 短时间高消耗
- 长时间无消耗
- 数据过期
- 额度重置
- ETA 晚于重置时间
- 修改轮询频率后 ETA 保持合理
- 自动轮询关闭后手动刷新

### 轮询

- 默认 5 分钟
- 30 秒
- 1 分钟
- 5 分钟
- 30 分钟
- 1 小时
- 自定义频率
- 非法频率
- 关闭自动轮询
- 手动刷新
- 连续点击手动刷新
- 请求超时
- 连续失败
- 指数退避
- 成功后恢复正常频率
- 窗口后台降频
- 窗口最小化暂停
- 系统睡眠
- 系统恢复
- 网络恢复后刷新
- 多窗口共享轮询
- 不产生重复定时器
- 不产生并发请求

### 补丁和恢复

- 首次安装
- 重复安装
- 安装失败回滚
- 卸载
- 重复卸载
- 恢复原始文件
- Codex 更新后检测
- 新版本不兼容时停止注入
- 重新应用补丁
- 回滚后 Codex 正常启动

---

## 二十三、验收标准

只有同时满足以下条件才算完成：

- 额度组件真实位于 Codex Desktop 顶部标题栏内部
- 位置位于菜单右侧、窗口控制按钮左侧的中央空白区域
- 不是底部状态栏
- 不是聊天内容区域
- 不是第二行工具栏
- 不是独立悬浮窗口
- 不增加标题栏高度
- 不覆盖菜单
- 不覆盖窗口控制按钮
- 不影响窗口拖动
- 不影响最大化、最小化和关闭
- 不影响 Windows Snap Layout
- 显示真实额度数据
- 不要求用户手动提供 Token 或 Cookie
- 显示剩余额度百分比
- 显示横向进度条
- 显示重置时间
- 基于历史数据显示预计耗尽时间
- 样本不足时不显示虚假 ETA
- ETA 晚于重置时间时显示本周期预计不会耗尽
- 支持多个额度窗口
- 支持亮色和暗色主题
- 支持窄窗口自适应
- 用户能够设置轮询频率
- 修改轮询频率后立即生效
- 支持关闭自动轮询
- 支持手动刷新
- 不产生重复定时器
- 不产生并发刷新
- 连续失败时自动退避
- 数据过期时明确标记
- 优先复用 Codex 客户端已有刷新结果
- Codex 原功能不受影响
- 支持安装、卸载、恢复和更新检测
- 修改失败可以自动回滚
- 不泄露认证信息、聊天内容或用户代码
- 完整源码和说明足以在本机重新构建和安装

---

## 二十四、执行流程

按以下阶段执行：

1. 读取 `AGENTS.md`
2. 检测本机 Codex Desktop
3. 识别安装来源和版本
4. 分析应用技术栈
5. 分析资源封装和完整性机制
6. 分析标题栏实现方式
7. 确认菜单和窗口控制按钮边界
8. 确认窗口拖动区域机制
9. 建立原始文件备份
10. 定位真实额度数据来源
11. 验证额度字段含义
12. 分析客户端已有刷新机制
13. 设计低侵入标题栏注入方案
14. 实现标题栏额度组件
15. 实现响应式布局
16. 实现详情面板
17. 实现多额度窗口管理
18. 实现历史采样
19. 实现 ETA 估算
20. 实现可配置轮询
21. 实现并发控制和失败退避
22. 实现配置持久化
23. 实现诊断能力
24. 实现安装、卸载和回滚
25. 完成自动化测试
26. 完成手工测试
27. 验证 Codex 原功能
28. 验证标题栏系统行为
29. 验证多显示器和高 DPI
30. 整理交付文档
31. 根据任务完成情况自行判断是否适合进行 Git commit

如果仓库是 Git 仓库，并且当前修改达到完整、可验证的阶段，应自行创建一次提交。

提交信息要求：

- 使用英文
- Header 必须存在
- Body 可选
- Footer 可选
- 不得提交 Codex 原始专有资源
- 不得提交认证信息
- 不得提交日志
- 不得提交备份文件
- 不得提交用户数据

遇到无法确认的内部字段、窗口行为或客户端机制时，不得自行编造。应继续通过本机文件、运行调试、实际响应和界面行为进行验证；确实无法确认的部分，在最终报告中明确标记。
```
